import { StyleSheet } from 'react-native';

export const authStyles = StyleSheet.create({
    imgView: {
        height: 100,
        display: 'flex',
        justifyContent: "center",
        alignItems: 'center',
        margin: 50
    },
    btnView: {
        height: 100,
        display: 'flex',
        justifyContent: "center",
        alignItems: 'center',
        margin: 10
      },
    logo: {
        borderWidth: 1,
        borderColor: 'black',
        borderRadius: 200
    }
});